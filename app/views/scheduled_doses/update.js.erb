
var sdClass = "panel panel-<%= @dose.panel_class %>"

var html = <% if @dose.side_effect == "" || @dose.side_effect.nil? %>
    ''
<% else %>
    'Side Effects Recorded'
<% end %>

var sd = new ScheduledDose(<%= @dose.id %>);
sd.update(html, sdClass);